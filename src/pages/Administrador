//const inicioCuatrimestre = new Date("2026-06-19");

/*  const MILISEGUNDOS_DIA = 1000 * 60 * 60 * 24;

  const fechasTutorias = eventos
    .filter(item => item.tipo.toLowerCase() === "tutoria")
    .map(item => new Date(item.fecha))
    .sort((a, b) => a - b);

  const inicioCuatrimestre = fechasTutorias[0];

  const primerLunes = new Date(inicioCuatrimestre);

  while (primerLunes.getDay() !== 1) {
    primerLunes.setDate(primerLunes.getDate() + 1);
  }
*/

      //const fecha = new Date(item.fecha);

      //let semana;

/*      if (fecha < primerLunes) {
        // Todo lo anterior al primer lunes pertenece a la semana 1
      semana = 1;
      } else {
        semana =
          Math.floor(
          (fecha - primerLunes) /
          (MILISEGUNDOS_DIA * 7)
        ) + 2;
      }*/
  /*const semanas = [
    ...new Set(
      eventos
        .filter(item => item.tipo.toLowerCase() === "tutoria")
        .map(item => {
          const fecha = new Date(item.fecha);
          if (fecha < primerLunes) {
            return 1;
          }

          return (
            Math.floor(
              (fecha - primerLunes) /
              (MILISEGUNDOS_DIA * 7)
            ) + 2
          );
        })
    )
  ].sort((a, b) => a - b);*/

      /*.filter(t => t.semana === semanaSeleccionada)
    .sort((a, b) => a.fechaReal - b.fechaReal);*/

    